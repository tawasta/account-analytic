name: Open PR from feature branch to dev

on:
  pull_request:
    branches:
      - 17.0
    types: [opened, reopened, synchronize]

jobs:
  open-pr-to-dev:
    uses: my-org/shared-workflows/.github/workflows/open-pr-to-dev.yml@main
    with:
      target_branch: 17.0-dev